import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        String str=sc.next();
        int k=sc.nextInt();
        int j=0;
        int max=0;
        for(int i=0;i<str.length();i++)
        {
            int count=0;
            for(j=i;j<=i+k-1 && j<str.length();j++)
            {
                if(str.charAt(j)=='a')
                {
                    count++;
                }
            }
